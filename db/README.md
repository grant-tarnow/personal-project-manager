# The db/ directory

PPM will use this directory to store sqlite3 databases.

Currently, PPM only uses a single db, `ppm.sqlite3`. You may make backups of this file or swap out this file as you please.
